<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>
	<!-- Struts 2的Action必须放在指定的包空间下定义 -->
	<package name="${objectName}" extends="common">

		<!-- copy  -->

		<!-- ${ModuleName} -->
		<action name="${ObjectName}Action" class="${ObjectName}Action">
			<result name="list">/modules/${objectName}/query.jsp</result>
			<result name="edit">/modules/${objectName}/edit.jsp</result>
			<result name="view">/modules/${objectName}/view.jsp</result>
		</action>

	</package>
</struts>